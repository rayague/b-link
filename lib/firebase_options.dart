// Minimal stub used until `flutterfire configure` generates real options.
// Keep this file intentionally tiny and free of special characters.

class DefaultFirebaseOptions {
  // The real generated file provides platform-specific FirebaseOptions.
  // Returning null here signals that Firebase isn't configured yet.
  static dynamic get currentPlatform => null;
}
